import { Database, SearchCheck, ShieldCheck, Sparkles } from "lucide-react";
import AppShell from "@/components/layout/AppShell";
import ChurchSearchAssistant from "@/components/assistant/ChurchSearchAssistant";
import { requireChurchModuleAccess } from "@/lib/modules/moduleAccess";
export const dynamic = "force-dynamic";
export default async function AssistantPage(){await requireChurchModuleAccess("ai_assistant");return <AppShell><div className="space-y-6"><section className="rounded-3xl bg-gradient-to-br from-[#03357A] via-[#2563EB] to-[#8B5CF6] p-6 text-white"><Sparkles className="h-9 w-9"/><h1 className="mt-4 text-3xl font-black">Assistant intelligent</h1><p className="mt-2 max-w-3xl text-sm leading-7 text-blue-50">Posez une question sur les membres, événements, départements, tâches, finances ou patrimoine. L’assistant recherche les sources autorisées, produit une synthèse et vous conduit vers les fiches concernées.</p></section><section className="grid gap-3 sm:grid-cols-3"><Feature icon={SearchCheck} title="Recherche transversale" text="Une question interroge plusieurs volets à la fois."/><Feature icon={Database} title="Sources vérifiables" text="Chaque résultat renvoie vers les données d’origine."/><Feature icon={ShieldCheck} title="Séparation stricte" text="Données limitées à votre église et à vos permissions."/></section><ChurchSearchAssistant/></div></AppShell>}

function Feature({icon:Icon,title,text}:{icon:any;title:string;text:string}){return <div className="rounded-2xl border border-[#DCEAF5] bg-white p-4"><Icon className="h-5 w-5 text-blue-600"/><p className="mt-3 font-black text-[#03357A]">{title}</p><p className="mt-1 text-xs leading-5 text-slate-500">{text}</p></div>}
